//! `sync-funcs-h` — Rust replacement for `tools/v2_sync_funcs_h.py`.
//!
//! Generates `recomp/funcs.h` from the bank cfgs. Lands in Phase 5.

fn main() {
    eprintln!("sync-funcs-h: not yet implemented (Phase 5).");
    std::process::exit(2);
}
